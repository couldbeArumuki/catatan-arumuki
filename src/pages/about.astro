---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getVisiblePosts } from '../lib/content';
import { buildSidebar } from '../lib/sidebar';
import { defaultAvatar, siteProfile } from '../lib/site';
import { withBase } from '../lib/urls';

const posts = await getVisiblePosts();
const sidebar = buildSidebar(posts);
---

<BaseLayout title="About" description="Learn about the diary behind Catatan Arumuki." sidebar={sidebar}>
  <section class="card about">
    <img src={withBase(defaultAvatar)} alt="Arumuki avatar illustration" width="220" height="220" />
    <div>
      <h2>Hi, I&apos;m {siteProfile.owner}</h2>
      <p>{siteProfile.aboutBio}</p>

      <h3>What I usually write about</h3>
      <div class="tags">
        {siteProfile.topics.map((topic) => (
          <span class="tag">{topic}</span>
        ))}
      </div>

      <h3>Find me here</h3>
      <ul class="list">
        {siteProfile.socials.map((social) => (
          <li>
            <a href={social.href} target="_blank" rel="noopener noreferrer">{social.label}</a>
          </li>
        ))}
      </ul>
    </div>
  </section>
</BaseLayout>

<style>
  .about {
    display: grid;
    grid-template-columns: minmax(0, 220px) minmax(0, 1fr);
    gap: 1.4rem;
  }

  .about img {
    width: 100%;
    border: 1px solid var(--border);
    background: var(--bg);
    padding: 0.25rem;
  }

  .about h3 {
    margin-bottom: 0.55rem;
  }

  @media (max-width: 720px) {
    .about {
      grid-template-columns: 1fr;
    }

    .about img {
      max-width: 220px;
    }
  }
</style>
